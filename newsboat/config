auto-reload yes
reload-threads 100
browser librewolf
# keybindings
unbind-key ENTER
unbind-key q
unbind-key Q
unbind-key UP
unbind-key DOWN
bind-key h quit
bind-key H hard-quit
bind-key j down
bind-key k up
bind-key l open
