#!/bin/sh

CONFIGS="\
mpd $HOME/.config/mpd/mpd.conf
nvim $HOME/.config/nvim/init.vim"

FILE=$(printf "$CONFIGS" \
	| $DMENU -l 10 \
	| cut -d ' ' -f 2)

if [ -n "$FILE" ]; then
	$TERMINAL -d none $EDITOR $FILE
fi
