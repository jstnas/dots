#!/bin/sh -e
VOLUME=$(pactl list sinks | grep '	Volume:' | awk '{print $5}')
echo $VOLUME
case $BUTTON in
    1)
        ~/bin/mute_volume
        ;;
    2)
        ~/bin/change_sink
        ;;
    3)
        notify-send 'Volume'
        ;;
esac
