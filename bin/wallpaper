#!/bin/sh -e
background_colour=#ffffff
wallpaper_directory=~/pictures/wallpapers/itadaki_yasu
pause=60

while true; do
	feh --no-fehbg --bg-max --randomize --image-bg $background_colour $wallpaper_directory
sleep $pause
done
