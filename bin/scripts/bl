#!/bin/sh

STEP=10

case $1 in
	M) backlight_control 100;;
	m) backlight_control 0;;
	+ | -) backlight_control $1$STEP;;
esac

BACKLIGHT="$(~/.local/bin/blocks/bl)"
notify-send -a 'backlight' -t 500 $BACKLIGHT
